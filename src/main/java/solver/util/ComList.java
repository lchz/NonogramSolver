
package solver.util;

/**
 *
 * @author lchz
 */
public class ComList {
        /**
     *  In Nonosolver, remove the uncommon cell
     */
//                    for (int index = 0; index < commons.length; index++) {
//                        if (commons[index] && !places.contains(index)) {
//                            commons[index] = false;
//                        }
// //                       if (!commons[index] && places.contains(index)) {
//   //                         commons[index] = true;
//     //                   }
//                    }
}
